// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  base: '/astro-blog/', // замените на имя вашего репозитория
  output: 'static',
});
